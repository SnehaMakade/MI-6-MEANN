testType: e2e #valid types are unit, e2e, load or failure
feature:
    name: "Employee"
    owner: "Onkar Bande"
    lastUpdate: "23 June, 2020"
    status: "production" #valid status are "Being defined", "development", "test", "production"
test:
    summary: "Record field should show first 50 records of department list"
    browsers:
        - "Chrome"
    steps:
        - "Go to HR -> Employee -> Department"
        - "Check spelling on all text contents"
        - "Change record field value to 50"
        - "Verify that there is 50 department list present at dashboard"
    conditions:
        - "Test fails if department list is not according to record field value"
    automation:
        status: manual #this test is automated or not
        testPath: #set this with a valid path if test is automated
testRun:
    status: pass #valid status are notRun, pass, fail, sometimeFail
    date: "23 June, 2020" #set the last when this test was run
    runBy: onkar #name of the person running this testType
    duration: 8 seconds #time taken in seconds to run this test
    env: qa #valid value are dev, qa, production
    buildVersion: xxx #mention the build version
    bug:
        details:
                "Multi time bug summary"
        bugNum: #if bug was reported and filed, report the bug number here

